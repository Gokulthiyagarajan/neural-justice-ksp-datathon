export default {
    apiBaseUrl: '/api',
    authTokenKey: 'auth_token',
    defaultPageSize: 20,
    maxPageSize: 100,
    requestTimeout: 30000,
    defaultCacheTTL: 300,
    featureFlags: {
        experimentalFeatures: false,
        newDashboard: true,
    }
}